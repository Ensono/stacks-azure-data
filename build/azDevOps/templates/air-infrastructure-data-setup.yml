parameters:
  TaskctlVersion: 1.5.0

steps:
  # Checkout self repo
  - checkout: self

  # Install Taskfile so that the tests can be run
  - task: Bash@3
    displayName: "Install: Taskctl"
    inputs:
      targetType: inline
      script: |
<<<<<<< HEAD
        wget https://github.com/Ensono/taskctl/releases/download/v${{ parameters.TaskctlVersion }}/taskctl_${{ parameters.TaskctlVersion }}_linux_amd64.tar.gz -O /tmp/taskctl.tar.gz
        tar zxf /tmp/taskctl.tar.gz -C /usr/local/bin taskctl
=======
        sudo wget https://github.com/taskctl/taskctl/releases/download/${{ parameters.TaskctlVersion }}/taskctl_${{ parameters.TaskctlVersion }}_linux_amd64.tar.gz -O /tmp/taskctl.tar.gz
        sudo tar zxf /tmp/taskctl.tar.gz -C /usr/local/bin taskctl
>>>>>>> main
